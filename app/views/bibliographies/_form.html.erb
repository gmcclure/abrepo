<%= form_for [@course, @bibliography] do |f| %>
  <% if @bibliography.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bibliography.errors.count, "error") %> prohibited this bibliography from being saved:</h2>

      <ul>
      <% @bibliography.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, 'Bibliography Title' %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit 'Update Title' %>
  </div>
<% end %>
