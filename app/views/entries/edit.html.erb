<div class="page-header">
  <h2>Update Entry</h2>
</div>

<%= render 'form' %>

<%= link_to 'Back', edit_course_bibliography_path(@bibliography.course, @bibliography) %>

<script>
  var rt_editor = function() {
    tinymce.init({
      content_css: '/assets/application.css',
      menu: { },
      selector: '#entry_annotation',
      width: 613
    });
  };

  $(document).ready(rt_editor);
  $(document).on('page:load page:restore', rt_editor);
</script>
