<%= form_for [@bibliography, @entry] do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :citation %>
    <%= f.text_area :citation %>
  </div>
  <div class="field">
    <%= f.label :annotation %>
    <%= f.text_area :annotation %>
  </div>
  <p>
  <div class="actions">
    <%= f.submit %>
  </div>
  </p>
<% end %>
